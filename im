Associate and remove associate back office reference association entity. Follow each step from the controller until the JPA entity for both the methods that have been mentioned below as part of limit portfolio controller. Follow the code from controller through service until the entity through adapter and repeat everything in a new or kind of duplicate whole thing into a new classes and files to implement the same logic using a new entity and entity structure is same as before and entity name is doctor. Have it in a new package in a similar method under new classes. Don't overwrite existing one or change anything in the existing one.
